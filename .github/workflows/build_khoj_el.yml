# melpa quality checks like checkdoc, byte-compile, package-lint for alphamind.el
# using melpazoid: https://github.com/riscy/melpazoid

name: build alphamind.el
on:
  push:
    branches:
      - 'master'
      - 'release/1.x'
    paths:
      - src/interface/emacs/*.el
      - .github/workflows/build_alphamind_el.yml
  pull_request:
    branches:
      - 'master'
      - 'release/1.x'
    paths:
      - src/interface/emacs/*.el
      - .github/workflows/build_alphamind_el.yml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.11
      uses: actions/setup-python@v1
      with: { python-version: 3.11 }
    - name: ⏬️ Install Dependencies
      run: |
        python -m pip install --upgrade pip
        sudo apt-get install emacs && emacs --version
        git clone https://github.com/riscy/melpazoid.git ~/melpazoid
        pip install ~/melpazoid
    - name: 🌡️ Validate AlphaMind.el
      env:
        # AlphaMind recipe from https://github.com/melpa/melpa/pull/8321/files
        RECIPE: (alphamind :fetcher github :repo "alphamind-ai/alphamind" :files ("src/interface/emacs/*.el"))
        EXIST_OK: true
        LOCAL_REPO: ${{ github.workspace }}
      run: echo $GITHUB_REF && make -C ~/melpazoid
